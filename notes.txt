main app (beanie, pydantic, fastapi, swagger, typescript, react, mongodb)
    -- start sechedule (mongodbjobstore)
    -- run api (fastapi, pydantic, swagger)
        -- frontend (typescript, react)
        -- crud jobs (mongodbjobstore, beanie)
        -- read documents (beanie)
    -- job execution (mongodbjobstore)